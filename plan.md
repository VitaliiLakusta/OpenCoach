Implement the next thing. Do not move to the next point until the previous is fully implemented and I confirm it. 

1. Create a feature where in frontend user tells path to the folder where to access notes (read/write). 
Agent will append the contents of the folder (all notes in it) in its context, in addition to user's query.